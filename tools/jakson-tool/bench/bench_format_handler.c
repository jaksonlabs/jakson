
#include "bench_format_handler.h"

bool bench_format_handler_create(bench_format_handler* handler, bench_error error, char* format_name) {
    //JAK_ERROR_IF_NULL(handler)
    //JAK_ERROR_IF_NULL(error)
    //JAK_ERROR_IF_NULL(format_name)
    JAK_UNUSED(handler);
    JAK_UNUSED(error);
    JAK_UNUSED(format_name);
    return false;
}
